import React from "react";

const BudgetSubmitButton = props => (
    <button className='btn btn-primary' type='' name='' onClick={props.onClick}>Submit</button>
)

export default BudgetSubmitButton;
